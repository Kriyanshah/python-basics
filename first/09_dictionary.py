m = {} # empty dictionary

marks = {
    "kriyan": 100, 
    "aksh": 60,
    "taksh": 33,
}
print(marks["kriyan"]) # prints the value of key kriyan
print(marks["aksh"]) # prints the value of key aksh
print(marks["taksh"]) # prints the value of key taksh

print(marks, type(marks)) # prints the dictionary and its type


"""dictionaries are mutable and ordered, they store data in key value pairs
   it is indexed and cannot duplicate keys but can have duplicate values"""